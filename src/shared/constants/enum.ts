export enum UserStatus {
  ACTIVE = 'active',
  BLOCKED = 'blocked',
}

export enum OtpType {
  SIGN_UP = 'sign_up',
  FORGOT_PASSWORD = 'forgot_password',
}

export enum DefaultStatus {
  ACTIVE = 'active',
  IN_ACTIVE = 'in_active',
}